package com.example.batallanaval.model;

/**
 * The orientation in which a ship can be placed on the board (see HU-1).
 *
 */
public enum Orientation {

    /** The ship extends to the right, sharing the same row. */
    HORIZONTAL,

    /** The ship extends downward, sharing the same column. */
    VERTICAL
}