package com.example.batallanaval.model;

/**
 * Identifies which side is acting or has won: the human player or the machine.
 *
 */
public enum Player {

    /** The human player. */
    HUMAN,

    /** The machine (computer) opponent. */
    MACHINE
}